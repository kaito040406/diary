from django import forms

# class nmaeForm(forms.Form):
#   name = forms.CharField(label='Nmae', max_length=20,required=True)

# class DiaryForm(forms.Form):
#   diary = forms.CharField(label='body', max_length=1000,required=True) 

# class CommentForm(forms.Form):
#   comment = forms.CharField(label='comment', max_length=1000,required=True)